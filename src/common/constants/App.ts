export enum HEADERS {
  API_KEY = 'api-key',
  AUTHORIZATION = 'Authorization',
  PREFIX_JWT_TOKEN = 'Bearer ',
}

export enum QUEUES {
  SCREEN_SHOOT = 'queue_screen_shoot',
  WEBHOOK = 'queue_webhook',
}
